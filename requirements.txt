SpeechRecognition>=3.10.0
# pyaudio is optional but recommended for better microphone support
# On macOS, install PortAudio first: brew install portaudio
# Then: pip install pyaudio
# pyaudio==0.2.14
